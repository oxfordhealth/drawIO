name: Find-Replace
on:
  pull_request:
    types:
      - closed
    branches:
      - 'main'
  workflow_dispatch:
jobs: 
  find-replace:
    runs-on: ubuntu-latest
    #if: github.event.pull_request.merged == true
    steps:
      - name: checkout repo
        uses: actions/checkout@latest
      - name: Test1
        run: ls ./
      - name: Test2
        run: ls ../
      - name: Test3
        run: ls ../../
      - name: Test4
        run: ls ../../../
      - name: Test5
        run: ls /
      #- name: Replace config
      #  run: sed -i 's/devBranch/main/' /drawioConfig.json;
